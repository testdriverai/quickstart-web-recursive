version: 4.2.12
session: 67a528251b6f3ddb9b034ef2
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
      - command: press-keys
        keys:
          - f6
      - command: type
        text: https://example.com
  - prompt: Navigate to the website URL.
    commands:
      - command: press-keys
        keys:
          - enter
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: the text "Example Domain" is visible on the page
