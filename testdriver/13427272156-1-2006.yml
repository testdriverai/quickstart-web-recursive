version: 4.2.12
session: 67b6a21f82f9d745f9cc4422
steps:
  - prompt: Locate the cookie consent bar at the bottom.
    commands:
      - command: hover-text
        text: Accept Cookies
        description: button in the cookie consent bar at the bottom
        action: hover
  - prompt: Click "Accept Cookies."
    commands:
      - command: hover-text
        text: Accept Cookies
        description: button in the cookie consent bar at the bottom
        action: click
  - prompt: Assert that the consent bar disappears.
    commands:
      - command: assert
        expect: the "Accept Cookies" button is not visible
  - prompt: Refresh the page and assert that the consent bar does not reappear.
    commands:
      - command: press-keys
        keys:
          - control
          - r
      - command: assert
        expect: the "Accept Cookies" button is not visible
  - prompt: Click on the "Start Building" button.
    commands:
      - command: hover-text
        text: Start Building
        description: button to initiate the building process
        action: click
  - prompt: Assert that the user is redirected to the expected page.
    commands:
      - command: assert
        expect: the page contains "Register for free"
