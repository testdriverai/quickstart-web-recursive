version: 4.2.12
session: 67a528281b6f3ddb9b034f02
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Assert that the "Start Building" button is visible on the homepage.
    commands:
      - command: assert
        expect: the "Start Building" button is visible
  - prompt: Click on the "Start Building" button.
    commands:
      - command: hover-text
        text: Start Building
        description: button to begin building on the homepage
        action: click
  - prompt: >-
      Assert redirection to the appropriate page or display of the intended
      content.
    commands:
      - command: assert
        expect: the page shows "Register for free"
