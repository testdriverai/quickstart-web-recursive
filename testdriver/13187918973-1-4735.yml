version: 4.2.12
session: 67a52598d06c99b9c179ceaa
steps:
  - prompt: Find the "Login" link at the top.
    commands:
      - command: hover-text
        text: LOGIN
        description: login link in the top right of the header
        action: hover
  - prompt: Click on "Login".
    commands:
      - command: hover-text
        text: LOGIN
        description: login link in the top right of the header
        action: click
  - prompt: >-
      Assert that the login page is displayed with the fields for username and
      password.
    commands:
      - command: assert
        expect: the login page displays fields for "Email address" and "Password"
