version: 4.2.12
session: 67b69f6c82f9d745f9cc43ab
steps:
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: the text "Example Domain" is visible on the page
  - prompt: Assert that the logo is visible.
    commands:
      - command: assert
        expect: the favicon is visible in the browser tab
