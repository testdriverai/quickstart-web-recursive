version: 4.2.12
session: 67bdf5488dfb545182f18418
steps:
  - prompt: Click on "Manage preferences" in the cookies notification.
    commands:
      - command: hover-text
        text: Manage preferences
        description: button in the cookies notification
        action: click
  - prompt: Assert that a preferences dialog opens.
    commands:
      - command: assert
        expect: Cookie preferences dialog is visible
  - prompt: Change a preference setting within the dialog.
    commands:
      - command: hover-text
        text: Analytical cookies
        description: toggle for analytical cookies in the preferences dialog
        action: click
  - prompt: Save the changes and close the dialog.
    commands:
      - command: hover-text
        text: Save preferences
        description: button in the cookie preferences dialog
        action: click
  - prompt: Click the "Sign In" button in the header.
    commands:
      - command: hover-text
        text: Sign In
        description: button in the header for signing in
        action: click
  - prompt: Assert that the sign in page or pop-up is displayed.
    commands:
      - command: assert
        expect: the sign-in dialog is visible
